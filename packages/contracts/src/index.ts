// Public surface of the API contract: the generated SDK functions, request/response
// types, and the configurable client. Regenerate with `pnpm gen` after changing routes;
// consumers set the base URL via `client.setConfig({ baseUrl })`.
export * from './generated'
export { client } from './generated/client.gen'
